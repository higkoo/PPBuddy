# WorkMate AI Agent 平台 - 开发指南

本文档为 WorkMate AI Agent 平台的分步骤开发指南，基于实际项目开发经验总结。

---

## 📋 项目开发完整流程

### 第一阶段：需求分析与文档准备

**目标**：明确产品愿景和功能范围

#### 步骤 1：产品需求文档（PRD）

创建 `/workspace/.trae/documents/PRD.md`，包含：

```markdown
## 1. 产品概述
- 项目背景：解决什么问题
- 核心价值：产品的独特卖点
- 目标用户：谁会使用这个产品

## 2. 功能模块
列出所有核心功能，按优先级排序

## 3. 用户流程
用流程图或时序图展示核心交互

## 4. MVP 定义
明确第一版本必须实现的功能
```

**关键要点**：
- 不要试图一次做所有功能
- 聚焦核心价值（MVP 思维）
- 考虑技术可行性

#### 步骤 2：技术架构文档

创建 `/workspace/.trae/documents/技术架构文档.md`，包含：

```markdown
## 1. 技术选型
- 前端框架：React / Vue
- 后端框架：Express / NestJS
- 数据库：SQLite / PostgreSQL
- 认证方案：JWT / OAuth

## 2. 系统架构
- 展示各层之间的关系
- 数据流向

## 3. API 设计
- RESTful 接口定义
- 请求/响应格式

## 4. 数据库设计
- ER 图
- DDL 语句
```

---

### 第二阶段：项目初始化

**目标**：搭建开发环境，创建项目骨架

#### 步骤 3：检查开发环境

```bash
# 检查 Node.js 版本
node -v  # 需要 >= 18

# 检查包管理器
pnpm -v  # 推荐使用 pnpm
npm -v    # 或使用 npm

# 检查 Git
git -v
```

#### 步骤 4：初始化项目

使用 Vite 快速创建项目：

```bash
# 使用 pnpm（推荐）
pnpm create vite-init@latest . --template react-express-ts --force

# 或使用 npm
npm init vite-init@latest -y . -- --template react-express-ts --force
```

**模板选择**：
- `react-ts`：纯前端 React 项目
- `react-express-ts`：React + Express 后端（全栈）
- `vue-ts`：纯前端 Vue 项目
- `vue-express-ts`：Vue + Express 后端（全栈）

#### 步骤 5：安装依赖

```bash
# 安装所有依赖
pnpm install

# 安装额外依赖（根据需要）
pnpm add axios zustand react-router-dom lucide-react
```

#### 步骤 6：配置项目

创建必要的配置文件：

**.env** - 环境变量
```env
# 数据库
DATABASE_PATH=./data/app.db

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=24h

# API 配置
API_BASE_URL=http://localhost:8000
```

**目录结构建议**：
```
/workspace
├── src/                    # 前端源码
│   ├── components/         # 组件
│   ├── pages/             # 页面
│   ├── stores/            # 状态管理
│   ├── api/               # API 调用
│   └── types/             # 类型定义
├── api/                    # 后端源码
│   ├── controllers/       # 控制器
│   ├── services/          # 服务层
│   ├── repositories/       # 数据访问层
│   ├── middleware/        # 中间件
│   └── routes/            # 路由
├── data/                   # 数据库文件
└── .env                    # 环境变量
```

---

### 第三阶段：后端开发

**目标**：实现核心 API 和业务逻辑

#### 步骤 7：数据库设计

创建数据库初始化脚本 `/workspace/api/database/init.ts`：

```typescript
import Database from 'better-sqlite3';
import path from 'path';

// 创建表结构
const createTables = (db: Database.Database) => {
  // 用户表
  db.exec(`
    CREATE TABLE IF NOT EXISTS users (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      email TEXT UNIQUE NOT NULL,
      password_hash TEXT NOT NULL,
      name TEXT NOT NULL,
      tenant_id INTEGER,
      role TEXT DEFAULT 'member',
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);

  // 租户表
  db.exec(`
    CREATE TABLE IF NOT EXISTS tenants (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      name TEXT NOT NULL,
      logo TEXT,
      config TEXT DEFAULT '{}',
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);

  // 更多表...
};
```

**关键要点**：
- 先设计 ER 图，再写 SQL
- 添加必要的索引
- 设置外键约束

#### 步骤 8：实现数据访问层

创建 Repository 文件 `/workspace/api/repositories/UserRepository.ts`：

```typescript
import { Database } from 'better-sqlite3';
import { User } from '../types';

export class UserRepository {
  constructor(private db: Database) {}

  create(email: string, passwordHash: string, name: string): User {
    const stmt = this.db.prepare(`
      INSERT INTO users (email, password_hash, name)
      VALUES (?, ?, ?)
    `);
    const result = stmt.run(email, passwordHash, name);
    return this.findById(result.lastInsertRowid as number);
  }

  findById(id: number): User | null {
    const stmt = this.db.prepare('SELECT * FROM users WHERE id = ?');
    return stmt.get(id) as User | null;
  }

  findByEmail(email: string): User | null {
    const stmt = this.db.prepare('SELECT * FROM users WHERE email = ?');
    return stmt.get(email) as User | null;
  }
}
```

#### 步骤 9：实现服务层

创建 Service 文件 `/workspace/api/services/AuthService.ts`：

```typescript
import bcrypt from 'bcrypt';
import jwt from 'jsonwebtoken';
import { UserRepository } from '../repositories/UserRepository';

export class AuthService {
  constructor(private userRepo: UserRepository) {}

  async register(email: string, password: string, name: string) {
    // 验证用户不存在
    const existing = this.userRepo.findByEmail(email);
    if (existing) {
      throw new Error('User already exists');
    }

    // 密码加密
    const passwordHash = await bcrypt.hash(password, 10);

    // 创建用户
    const user = this.userRepo.create(email, passwordHash, name);

    // 生成 Token
    const token = jwt.sign(
      { userId: user.id, email: user.email },
      process.env.JWT_SECRET!,
      { expiresIn: process.env.JWT_EXPIRES_IN }
    );

    return { user, token };
  }

  async login(email: string, password: string) {
    const user = this.userRepo.findByEmail(email);
    if (!user) {
      throw new Error('Invalid credentials');
    }

    const isValid = await bcrypt.compare(password, user.password_hash);
    if (!isValid) {
      throw new Error('Invalid credentials');
    }

    const token = jwt.sign(
      { userId: user.id, email: user.email },
      process.env.JWT_SECRET!,
      { expiresIn: process.env.JWT_EXPIRES_IN }
    );

    return { user, token };
  }
}
```

#### 步骤 10：实现控制器层

创建 Controller 文件 `/workspace/api/controllers/AuthController.ts`：

```typescript
import { Request, Response } from 'express';
import { AuthService } from '../services/AuthService';

export class AuthController {
  constructor(private authService: AuthService) {}

  register = async (req: Request, res: Response) => {
    try {
      const { email, password, name, tenantName } = req.body;
      const result = await this.authService.register(email, password, name);
      res.json({ success: true, data: result });
    } catch (error: any) {
      res.status(400).json({ success: false, error: error.message });
    }
  };

  login = async (req: Request, res: Response) => {
    try {
      const { email, password } = req.body;
      const result = await this.authService.login(email, password);
      res.json({ success: true, data: result });
    } catch (error: any) {
      res.status(401).json({ success: false, error: error.message });
    }
  };
}
```

#### 步骤 11：创建中间件

创建认证中间件 `/workspace/api/middleware/auth.ts`：

```typescript
import { Request, Response, NextFunction } from 'express';
import jwt from 'jsonwebtoken';

export interface AuthRequest extends Request {
  user?: { userId: number; email: string };
}

export const authenticate = (
  req: AuthRequest,
  res: Response,
  next: NextFunction
) => {
  try {
    const token = req.headers.authorization?.replace('Bearer ', '');
    if (!token) {
      return res.status(401).json({ error: 'No token provided' });
    }

    const decoded = jwt.verify(token, process.env.JWT_SECRET!) as {
      userId: number;
      email: string;
    };
    req.user = decoded;
    next();
  } catch (error) {
    res.status(401).json({ error: 'Invalid token' });
  }
};
```

#### 步骤 12：配置路由

创建路由文件 `/workspace/api/routes/auth.ts`：

```typescript
import { Router } from 'express';
import { AuthController } from '../controllers/AuthController';
import { authenticate } from '../middleware/auth';

const router = Router();
const authController = new AuthController();

router.post('/register', authController.register);
router.post('/login', authController.login);
router.get('/me', authenticate, authController.getProfile);

export default router;
```

---

### 第四阶段：前端开发

**目标**：实现用户界面和交互

#### 步骤 13：配置路由

创建 `/workspace/src/App.tsx`：

```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { useAuthStore } from './stores/authStore';
import Login from './pages/Login';
import Dashboard from './pages/Dashboard';

function PrivateRoute({ children }: { children: React.ReactNode }) {
  const { isAuthenticated } = useAuthStore();
  return isAuthenticated ? children : <Navigate to="/" />;
}

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Login />} />
        <Route
          path="/dashboard"
          element={
            <PrivateRoute>
              <Dashboard />
            </PrivateRoute>
          }
        />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

#### 步骤 14：创建状态管理

创建 Zustand Store `/workspace/src/stores/authStore.ts`：

```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import { authApi } from '../api/auth';
import { User } from '../types';

interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
  register: (email: string, password: string, name: string) => Promise<void>;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      token: null,
      isAuthenticated: false,

      login: async (email: string, password: string) => {
        const response = await authApi.login(email, password);
        set({
          user: response.data.user,
          token: response.data.token,
          isAuthenticated: true,
        });
      },

      logout: () => {
        set({ user: null, token: null, isAuthenticated: false });
      },

      register: async (email: string, password: string, name: string) => {
        const response = await authApi.register(email, password, name);
        set({
          user: response.data.user,
          token: response.data.token,
          isAuthenticated: true,
        });
      },
    }),
    {
      name: 'auth-storage',
    }
  )
);
```

#### 步骤 15：创建登录页面

创建 `/workspace/src/pages/Login.tsx`：

```tsx
import { useState } from 'react';
import { useAuthStore } from '../stores/authStore';

export default function Login() {
  const [isLogin, setIsLogin] = useState(true);
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [name, setName] = useState('');
  const { login, register } = useAuthStore();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    try {
      if (isLogin) {
        await login(email, password);
      } else {
        await register(email, password, name);
      }
    } catch (error) {
      console.error('Auth error:', error);
    }
  };

  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <div className="bg-white p-8 rounded-lg shadow-lg w-96">
        <h1 className="text-2xl font-bold mb-6 text-center">
          {isLogin ? '登录' : '注册'}
        </h1>
        <form onSubmit={handleSubmit} className="space-y-4">
          {!isLogin && (
            <input
              type="text"
              placeholder="名字"
              value={name}
              onChange={(e) => setName(e.target.value)}
              className="w-full p-2 border rounded"
            />
          )}
          <input
            type="email"
            placeholder="邮箱"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            className="w-full p-2 border rounded"
          />
          <input
            type="password"
            placeholder="密码"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            className="w-full p-2 border rounded"
          />
          <button
            type="submit"
            className="w-full bg-blue-600 text-white p-2 rounded hover:bg-blue-700"
          >
            {isLogin ? '登录' : '注册'}
          </button>
        </form>
        <p className="mt-4 text-center text-sm">
          {isLogin ? '没有账号？' : '已有账号？'}
          <button
            onClick={() => setIsLogin(!isLogin)}
            className="text-blue-600 hover:underline"
          >
            {isLogin ? '注册' : '登录'}
          </button>
        </p>
      </div>
    </div>
  );
}
```

#### 步骤 16：创建对话页面

创建 `/workspace/src/pages/Dashboard.tsx`：

```tsx
import { useState, useEffect } from 'react';
import { chatApi } from '../api/chat';
import { useChatStore } from '../stores/chatStore';
import MessageBubble from '../components/MessageBubble';
import ChatInput from '../components/ChatInput';

export default function Dashboard() {
  const [input, setInput] = useState('');
  const { messages, addMessage, isLoading, setLoading } = useChatStore();

  const handleSend = async () => {
    if (!input.trim()) return;

    setLoading(true);
    addMessage({ role: 'user', content: input });

    try {
      await chatApi.sendMessage({ message: input });
      addMessage({ role: 'assistant', content: '思考中...' });
    } catch (error) {
      console.error('Send error:', error);
    } finally {
      setLoading(false);
      setInput('');
    }
  };

  return (
    <div className="flex h-screen">
      <aside className="w-64 bg-gray-100 p-4">
        <button className="w-full bg-blue-600 text-white p-2 rounded mb-4">
          新建对话
        </button>
        {/* 对话历史列表 */}
      </aside>
      <main className="flex-1 flex flex-col">
        <div className="flex-1 overflow-auto p-4">
          {messages.map((msg, i) => (
            <MessageBubble key={i} message={msg} />
          ))}
        </div>
        <div className="p-4 border-t">
          <ChatInput
            value={input}
            onChange={setInput}
            onSend={handleSend}
            disabled={isLoading}
          />
        </div>
      </main>
    </div>
  );
}
```

---

### 第五阶段：测试与优化

**目标**：确保代码质量，发现并修复问题

#### 步骤 17：类型检查

```bash
# 运行 TypeScript 检查
pnpm run check
```

#### 步骤 18：构建项目

```bash
# 构建生产版本
pnpm run build
```

#### 步骤 19：启动开发服务器

```bash
# 启动前端开发服务器
pnpm run client:dev

# 启动后端开发服务器
pnpm run server:dev

# 同时启动前后端
pnpm run dev
```

---

## 🔧 常见问题与解决方案

### 1. 数据库连接失败

**问题**：无法连接到 SQLite 数据库

**解决**：
```typescript
// 确保目录存在
import fs from 'fs';
const dbDir = path.dirname(process.env.DATABASE_PATH!);
if (!fs.existsSync(dbDir)) {
  fs.mkdirSync(dbDir, { recursive: true });
}
```

### 2. CORS 问题

**问题**：前端无法调用后端 API

**解决**：在后端配置 CORS
```typescript
import cors from 'cors';
app.use(cors({
  origin: 'http://localhost:5173', // 前端地址
  credentials: true
}));
```

### 3. JWT Token 过期

**问题**：请求返回 401

**解决**：在客户端处理 token 刷新
```typescript
// axios 拦截器
apiClient.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      // 清除本地存储并重定向到登录页
      localStorage.removeItem('token');
      window.location.href = '/';
    }
    return Promise.reject(error);
  }
);
```

---

## 📚 学习资源推荐

### 官方文档
- [React 官方文档](https://react.dev)
- [Express 官方文档](https://expressjs.com)
- [TypeScript 官方文档](https://www.typescriptlang.org)

### 工具文档
- [Tailwind CSS](https://tailwindcss.com)
- [Zustand](https://zustand-demo.pmnd.rs)
- [Better-SQLite3](https://github.com/WiseLibs/better-sqlite3)

### 实践建议
1. **小步前进**：每次只做一个小功能
2. **持续测试**：每个功能完成后立即测试
3. **代码审查**：写完代码后自己审查一遍
4. **记录问题**：遇到的问题和解决方案要记录

---

## 🚀 部署建议

### 开发环境
```bash
pnpm run dev  # 同时启动前后端
```

### 生产环境
```bash
# 1. 构建前端
pnpm run build

# 2. 构建后端
# 使用 pm2 或 docker 部署

# 3. 配置环境变量
# 确保生产环境的 JWT_SECRET 和 DATABASE_PATH 正确设置
```

---

## 总结

项目开发的 5 个核心阶段：

1. **需求分析**：明确做什么（PRD）
2. **技术设计**：确定怎么做（架构文档）
3. **后端开发**：实现 API 和业务逻辑
4. **前端开发**：实现用户界面
5. **测试优化**：确保质量和性能

遵循这些步骤，你就能系统地完成一个完整的项目开发！
